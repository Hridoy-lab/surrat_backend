from django.apps import AppConfig


class AudioToTranscribeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "audio_to_transcribe"
    verbose_name = "Audio To Transcribe"
